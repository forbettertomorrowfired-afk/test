{
	frankenphp
	order php_server before file_server
}

# Force HTTP (Railway handles HTTPS termination). Use absolute path for safety.
http://:{$PORT:8080} {
	root * /app/public
	
	encode zstd gzip
	
	rewrite /login /index.php
	
	php_server
}
